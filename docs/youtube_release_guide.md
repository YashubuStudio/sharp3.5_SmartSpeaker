# YouTube台本：AIスマートスピーカーのセットアップガイド

## 動画情報
- **タイトル案**: 「AIスマートスピーカーをあなたのPCで動かそう！【セットアップ完全ガイド】」
- **想定尺**: 10〜12分
- **対象視聴者**: 7zファイルを受け取って実際にセットアップ・利用する人

---

## オープニング（0:00〜1:00）

こんにちは、AIコンシェルジュを作ろうです。

今回は、私が作っているAIスマートスピーカーを皆さんのPCで動かす方法を説明します。

これは何かというと、PCのマイクに向かって話しかけると、AIが考えて音声で返事してくれるアプリケーションです。
ウェイクワードを言うと起動して、質問すればAIが日本語で答えてくれます。
ウェイクワードは自分の好きな言葉に変えられるので、その方法も後で説明します。
処理は全部あなたのPC上で動くので、クラウドに音声データが送られることはありません。

配布した7zファイルを展開して、 **batファイルを2つ叩くだけ** で使えるようになります。
順番に説明していきます。

---

## しくみの概要（1:00〜2:00）

まず簡単にしくみを説明します。
このアプリケーションは以下の流れで処理が進みます。

```
マイク → ウェイクワード検出 → 音声録音 → 音声認識 → AI応答生成 → 音声合成 → スピーカー
```

また、裏ではこの3つのサービスが動いています：

| サービス | 役割 | 説明 |
|---------|------|------|
| Ollama | AIの頭脳 | 質問を理解して返答を考える |
| VOICEVOX | 声 | AIの返答を日本語の音声に変換する |
| Whisper | 耳 | あなたの声をテキストに変換する |

セットアップではこれらをインストールしていきますが、ほぼ自動なので安心してください。

---

## どのバージョンを使う？（2:00〜3:00）

Githubに保存した7zファイルは3種類あります。自分のPCに合ったものを選んでください。

### CPU版（smart_speaker-v0.1.0-cpu.7z）
- **NVIDIA GPUがない人はこれ**
- どのWindows PCでも動く
- 音声認識が遅い

### CUDA-bundled版（smart_speaker-v0.1.0-cuda-bundled.7z）
- **NVIDIA GPU搭載ならこれがおすすめ**
- GPU用のファイルが全部入っているのでそのまま動く
- 音声認識が高速

### CUDA版（smart_speaker-v0.1.0-cuda.7z）
- NVIDIA GPU搭載 + CUDA Toolkitを自分でインストールしたい人向け
- 上級者向けなので、迷ったらCUDA-bundled版を使ってください

**判断基準はシンプルです：NVIDIA GPU持ってる → CUDA-bundled版、持ってない → CPU版。**

---

## 手順1: 7zファイルを展開する（3:00〜3:30）

まず、受け取った7zファイルを展開します。

7-Zipがインストールされていれば右クリック →「展開」でOKです。
持っていなければ https://www.7-zip.org/ から無料でインストールできます。

展開先はデスクトップでもDドライブでも、どこでも大丈夫です。

展開するとこんなフォルダ構成になっています：
ウェイクワードを変えない場合は、batファイルをダブルクリックする以外にやることはありません。

```
smart_speaker-v0.1.0-cuda-bundled/
├── smart_speaker.exe    ← 本体
├── setup.bat            ← 最初に1回だけ実行
├── run.bat              ← 毎回これで起動
├── config/
│   └── settings.toml    ← 設定ファイル（後で調整可能）
├── models/              ← Whisperモデルの保存先
├── data/
│   └── knowledge/       ← AIに覚えさせたい情報を入れる場所
└── wakeword.rpw         ← ウェイクワードの認識モデル
```

---

## 手順2: setup.batを実行する（3:30〜5:30）

展開したフォルダの中にある **`setup.bat`** をダブルクリックしてください。

（画面にsetup.batの実行を映す）

これが自動的に必要なものをインストールしていきます。

**[1/6] フォルダ作成** — 一瞬で終わります

**[2/6] GPU確認**（GPU版のみ）
- CUDA-bundled版なら必要なファイルが同梱済みなので問題なく通ります

**[3/6] Ollamaのインストール**
- AIの頭脳にあたるサービスです
- 入っていなければインストーラーを自動ダウンロードして実行します
- Ollamaのインストール画面が出たら、指示に従って進めてください

**[4/6] AIモデルのダウンロード**
- `gemma3:4b`（AIの応答用、約2.5GB）
- `nomic-embed-text`（知識検索用）
- ネット回線によりますが、数分かかります

**[5/6] Whisperモデルのダウンロード**
- 音声認識用のモデルファイルです（約1.6GB）
- 自動でダウンロードされます

**[6/6] VOICEVOXの確認**
- ここで「VOICEVOX not found」と表示されたら、次の手順で手動インストールします

全体で **10〜20分** くらいかかります。ほとんどはダウンロードの待ち時間です。

---

## 手順3: VOICEVOXをインストールする（5:30〜6:30）

VOICEVOXだけは自動インストールできないので、手動でお願いします。

1. https://voicevox.hiroshiba.jp/ にアクセス
2. 「ダウンロード」をクリック
3. Windows版をダウンロードしてインストール
4. インストールが終わったら起動

起動すると画面が出ますが、閉じなければOKです。タスクトレイに常駐して裏で動き続けます。

**VOICEVOXはスマートスピーカーを使うときに毎回起動しておく必要があります。**
（run.batが自動で起動を試みますが、インストールだけは先にやっておいてください）

---

## 手順4: run.batで起動する（6:30〜8:00）

ここまで来たらあとは簡単です。

**`run.bat`** をダブルクリックしてください。

（画面にrun.batの実行を映す）

run.batが裏側のサービスを確認・起動して、スマートスピーカー本体を立ち上げます。

```
[1/3] Checking Ollama...
      Ollama: Running
[2/3] Checking VOICEVOX...
      VOICEVOX: Running

  --- Service Status ---
  Ollama:   OK
  VOICEVOX: OK
  --------------------

[3/3] Launching Smart Speaker...
```

この表示が出れば準備完了です。

### 使い方

1. **「さくら」** と呼びかける（ウェイクワード）
2. ピッという反応があったら、質問を話す
3. AIが考えて、音声で返事してくれる

例えば「さくら、今日の天気教えて」「さくら、カレーの作り方は？」のように話しかけてみてください。
(今はgemma3:4bの知識と過去の会話ログからしか回答してくれないですが、今後はWeb検索や知識も充実させていきたいですね。)

また。**2回目以降はrun.batをダブルクリックするだけです。** setup.batは最初の1回だけでOK。

---

## ウェイクワードを自分好みに変える（8:00〜9:00）

デフォルトでは「さくら」がウェイクワードになっていますが、好きな言葉に変更できます。

### 作り方

1. ブラウザで以下のサイトにアクセスします
   https://givimad.github.io/rustpotter-create-model-demo/
2. 自分の好きなウェイクワードをマイクに向かって **3回** 録音します
   - 例：「ねえアリス」「オッケーコンピュータ」など
   - 静かな環境でハッキリ発音するのがコツです
3. 録音が終わったら `.rpw` ファイルをダウンロード

### 設定方法

1. ダウンロードした `.rpw` ファイルをスマートスピーカーのフォルダに入れる
2. `config/settings.toml` をメモ帳で開く
3. `[wakeword]` セクションの `wakeword_path` を変更する

```toml
[wakeword]
wakeword_path = "my_wakeword.rpw"   # ← ダウンロードしたファイル名に変更
threshold = 0.35
avg_threshold = 0.15
min_scores = 1
```

4. run.batで再起動すれば、新しいウェイクワードで動きます

うまく反応しない場合は `threshold` を `0.25` くらいに下げてみてください。
逆に誤反応が多い場合は `0.45` くらいに上げると改善します。

---

## うまく動かないときは（9:00〜10:30）

よくあるトラブルをいくつか紹介しますが、ここに載っていない問題が出たら、遠慮なく **この動画のコメント欄** で聞いてください。エラーメッセージを貼ってもらえると対応しやすいです。

### 「Ollama not found」と出る
→ setup.batを先に実行してください。それでもダメなら https://ollama.com/ から手動インストール。

### 「VOICEVOX: Failed to start」と出る
→ VOICEVOXがインストールされていないか、起動していません。インストールして起動してからrun.batを再実行。

### Whisperモデルのダウンロードが途中で止まる
→ ネットワークの問題です。再度setup.batを実行すれば途中から再開します。
→ どうしてもダメなら、ブラウザで直接ダウンロードして `models/` フォルダに入れてください。

### ウェイクワードに反応しない
→ PCのマイクがオンになっているか確認してください。
→ Windowsの「設定 → システム → サウンド → 入力」でマイクが選択されているかチェック。
→ それでもダメなら `config/settings.toml` の `threshold` を `0.25` に下げてみてください。

### 反応が遅い
→ CPU版を使っている場合、音声認識に時間がかかります。NVIDIA GPU搭載PCならCUDA-bundled版を試してください。
→ Ollamaのモデルを軽いものに変えることもできます（`config/settings.toml` の `model` を変更）。

### その他のトラブル
→ **この動画のコメント欄に書いてください。** 画面に表示されているエラーメッセージをそのまま貼ってもらえると、すぐに原因がわかることが多いです。

---

## まとめ（10:30〜11:00）

もう一度手順をおさらいします。

1. **7zファイルを展開**
2. **setup.bat を実行**（初回のみ、10〜20分）
3. **VOICEVOXをインストール**（手動、初回のみ）
4. **run.bat を実行**（毎回これだけ）
5. **「さくら」と話しかける**

何かつまずいたら、コメント欄にエラーメッセージと一緒に書き込んでください。できる限り対応します。
それでは、楽しんで使ってもらえたら嬉しいです！

---

## 概要欄テンプレート

```
AIスマートスピーカーのセットアップ方法を解説します。
配布された7zファイルを展開して、batファイルを実行するだけで使えます。

▼ セットアップ手順
1. 7zファイルを展開
2. setup.bat を実行（自動インストール）
3. VOICEVOXを手動インストール
4. run.bat で起動
5.「さくら」と話しかける

▼ 必要なもの（setup.batが自動インストール）
- Ollama: https://ollama.com/
- Whisperモデル（自動ダウンロード）

▼ 手動インストールが必要なもの
- VOICEVOX: https://voicevox.hiroshiba.jp/
- 7-Zip（展開用）: https://www.7-zip.org/

▼ タイムスタンプ
0:00 オープニング
1:00 しくみの概要
2:00 どのバージョンを使う？
3:00 手順1: 7zファイルを展開
3:30 手順2: setup.batを実行
5:30 手順3: VOICEVOXをインストール
6:30 手順4: run.batで起動・使い方
8:00 ウェイクワードを自分好みに変える
9:00 うまく動かないときは
10:30 まとめ

#AI #スマートスピーカー #音声アシスタント #VOICEVOX
```
